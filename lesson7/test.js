// ==========================================
// ARCHIVO JS TRANSPIILADO
// ==========================================
// Este archivo probablemente fue generado automáticamente por el compilador de TypeScript (tsc).
// Muestra cómo el código TS se convierte a JS estándar para que el navegador lo entienda.
// Nota como los tipos (: number) han desaparecido.

var a = 5; // En versiones antiguas (ES5), 'var' era la única forma de declarar variables.
var b = 10;

function calSum(param1, param2) {
    var result = param1 + param2;
    console.log(result);
}
calSum(a, b);
